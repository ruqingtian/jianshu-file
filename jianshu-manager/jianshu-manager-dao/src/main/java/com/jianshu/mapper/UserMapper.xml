<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jianshu.mapper.UserMapper" >
    <resultMap id="BaseResultMap" type="com.jianshu.pojo.User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="nickname" property="nickName" jdbcType="VARCHAR"/>
        <result column="username" property="userName" jdbcType="VARCHAR"/>
        <result column="pwd" property="pwd" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="mail" property="mail" jdbcType="VARCHAR"/>
        <result column="user_desc" property="userDesc" jdbcType="VARCHAR"/>
        <result column="fans_nums" property="fansNums" jdbcType="INTEGER"/>
        <result column="concern_nums" property="concernNums" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="img" property="img" jdbcType="VARCHAR"/>

    </resultMap>
    <insert id="insert" parameterType="com.jianshu.pojo.User">
        INSERT INTO user
        (nickname,username,pwd,sex,phone,mail,create_time,update_time,img)
        VALUES
         (#{nickName,jdbcType=VARCHAR},#{userName,jdbcType=VARCHAR},#{pwd,jdbcType=VARCHAR},
         #{sex,jdbcType=VARCHAR},#{phone,jdbcType=VARCHAR},#{mail,jdbcType=VARCHAR},
         #{createTime,jdbcType=TIMESTAMP},#{updateTime,jdbcType=TIMESTAMP},#{img,jdbcType=VARCHAR})
    </insert>
    <select id="selectAllUser" resultMap="BaseResultMap">
        select id,nickname,username,pwd,sex,phone,mail,user_desc,fans_nums,concern_nums,create_time,update_time,img
        FROM user
    </select>
    <select id="selectByUserName"  resultType="com.jianshu.pojo.User">
        SELECT * FROM user WHERE username=#{userName,jdbcType=VARCHAR}
    </select>
    <select id="selectUserById" parameterType="Integer" resultMap="BaseResultMap">
        SELECT id,nickname,username,pwd,sex,phone,mail,user_desc,fans_nums,concern_nums,create_time,update_time,img
        FROM USER
        WHERE id=#{userId}
    </select>

    <!--分页查询主页显示user-->
    <select id="selectPageUser" resultMap="BaseResultMap">
        select id,nickname,fans_nums,concern_nums,img
        FROM user limit #{index},#{currentCount}
    </select>
    <!--查询总条数-->
    <select id="selectTotalCount" resultType="Integer">
        SELECT count(id) from user
    </select>
   <!-- <update id="update" parameterType="com.jianshu.pojo.User">
        UPDATE user  SET
        nickname=#{nickName,jdbcType=VARCHAR},username=#{userName,jdbcType=VARCHAR},pwd=#{pwd,jdbcType=VARCHAR},
        sex=#{sex,jdbcType=VARCHAR},phone=#{phone,jdbcType=VARCHAR},mail=#{mail,jdbcType=VARCHAR},user_desc=#{userDesc,jdbcType=VARCHAR},
        fans_nums=#{fansNums,jdbcType=INTEGER},concern_nums=#{concernNums,jdbcType=INTEGER},update_time=#{updateTime,jdbcType=TIMESTAMP}
        where id=#{id,jdbcType=INTEGER}
    </update>-->


</mapper>

